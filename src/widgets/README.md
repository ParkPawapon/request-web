# Widgets

Widgets are composed UI blocks such as app header, sidebar, request summary
panels, dashboard sections, and filter bars.

Rules:
- Widgets compose entities and shared UI.
- Widgets should stay workflow-agnostic unless promoted into `processes`.
- Route registration remains in `app`.
